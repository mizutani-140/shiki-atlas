# Use AGENTS As The Shared Agent Constitution

Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.
